{$but_text=$but_text|default:__("register")}
{$but_meta=$but_meta|default:"ty-btn__secondary"}
{include file="buttons/button.tpl" but_text=$but_text but_onclick=$but_onclick but_href=$but_href but_target=$but_target but_meta=$but_meta but_role=$but_role}